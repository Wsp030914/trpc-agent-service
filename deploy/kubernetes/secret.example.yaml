apiVersion: v1
kind: Secret
metadata:
  name: trpc-agent-service-secrets
type: Opaque
stringData:
  TRPC_AGENT_SERVICE_POSTGRES_DSN: postgres://user:replace-me@postgres:5432/trpc_agent_service?sslmode=disable
  TRPC_AGENT_SERVICE_ADMIN_TOKEN: replace-me-with-an-external-secret
